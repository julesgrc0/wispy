#include "player.h"

void update_player(w_player *player, float velocity, float dt)
{
  // if (IsKeyDown(KEY_RIGHT) && player->velocity.x < (velocity / 2))
  // {
  //   player->velocity.x = velocity;
  //   player->direction = 0;
  // }
  // else if (IsKeyDown(KEY_LEFT) && player->velocity.x > -(velocity / 2))
  // {
  //   player->velocity.x = -velocity;
  //   player->direction = 1;
  // }

  // if (player->delay > 0)
  // {
  //   player->delay -= dt;
  // }
  // else
  // {
  //   if (IsKeyDown(KEY_SPACE) && player->onground == 1)
  //   {
  //     player->velocity.y = -(velocity * 20);
  //     player->delay = 0.3f;
  //     player->onground = 0;
  //   }
  //   else if (player->velocity.y >= 0 && player->onground == 0)
  //   {
  //     player->velocity.y = velocity * 5;
  //   }
  // }
}
